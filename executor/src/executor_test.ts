import assert from "node:assert/strict";
import { describe, it } from "node:test";
// import { Executor } from "./Executor.js";

describe("test", () => {
  it("works", () => {
    assert.ok(true);
  });
});
// TODO
// describe("executor.managesItem()", () => {
//   it("works", () => {
//     const capacity = 2;
//     const index = 1;
//     const itemId = "aaaaaaaa-aaaa-aaaa-aaaa-aaaaaaaaaaaa";
//     const executor = new Executor(capacity, index);
//     assert.ok(executor.managesItem(itemId));
//   });
// });
