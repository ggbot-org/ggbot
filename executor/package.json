{
  "name": "@ggbot2/executor",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build tsconfig.build.json",
    "test": "npm run tsc--noEmit",
    "tsc--noEmit": "tsc --noEmit --project .",
    "start": "node ./dist/daemon.js"
  },
  "dependencies": {
    "@ggbot2/dflow": "file:dflow"
  },
  "devDependencies": {
    "@types/node": "^18.0.3",
    "typescript": "^4.7.4"
  }
}
