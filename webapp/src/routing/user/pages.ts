// TODO remove this file
import { webappDirname } from "@workspace/locators"

const { user } = webappDirname

export const userDashboardHtmlPathname = `/${user}/dashboard.html`

export const settingsHtmlPathname = `/${user}/settings.html`
