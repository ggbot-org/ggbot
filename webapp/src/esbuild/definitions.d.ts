import { DeployStage } from '@workspace/env'

export declare const esbuildDefinitions: {
	BUILD_DATE: string;
	DEPLOY_STAGE: DeployStage;
	DNS_DOMAIN: string;
	PROJECT_SHORT_NAME: string;
	STRIPE_PLAN_BASIC_MONTHLY_PRICE: string;
}
