{
  "name": "@workspace/api-stripe-webhook",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "check_types": "tsc --build",
    "cleanup": "rm -rf dist"
  },
  "dependencies": {
    "@workspace/api": "0.0.0",
    "@workspace/database": "0.0.0",
    "@workspace/env": "0.0.0",
    "@workspace/models": "0.0.0",
    "@workspace/stripe": "0.0.0",
    "@workspace/s3-data-bucket": "0.0.0",
    "minimal-time-helpers": "0.1.0"
  },
  "devDependencies": {
    "@workspace/tsconfig": "0.0.0"
  }
}
