{
  "name": "ggbot2",
  "description": "crypto flow",
  "homepage": "http://ggbot2.com",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "admin-webapp": "npm run dev --workspace admin-webapp",
    "build:admin-context": "npm run build --workspace admin-context",
    "build:admin-webapp": "npm run build --workspace admin-webapp",
    "build:aws": "npm run build --workspace aws",
    "build:database": "npm run build --workspace database",
    "build:env": "npm run build --workspace env",
    "build:models": "npm run build --workspace models",
    "lint": "npm run lint --workspaces --if-present",
    "prebuild:admin-webapp": "run-s build:database",
    "prebuild:aws": "run-s build:env build:models",
    "prebuild:database": "run-s build:aws",
    "test": "npm run test --workspaces --if-present",
    "tsc--noEmit": "npm run tsc--noEmit --workspaces --if-present"
  },
  "workspaces": [
    "admin-context",
    "admin-webapp",
    "aws",
    "database",
    "env",
    "models"
  ],
  "keywords": [],
  "author": "Gianluca Casati (http://fibo.github.io/)",
  "license": "UNLICENSED",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
